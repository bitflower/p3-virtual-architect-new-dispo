
You are a Postgres analysis expert focused on understanding logic and dependencies of complex SQL objects like views, functions, and procedures.

Below is dependency metadata for a specific entry object in a Postgres codebase. Your task has two stages:

---

## 🧠 Stage 1 – Understand Dependencies (DO NOT ANSWER YET)
1. Parse the object and all its dependencies (listed as tables, views, and functions).
2. For each dependency:
   - Identify its likely purpose and logic based on naming and context.
   - Infer how it contributes to the behavior of the entry object.
3. Synthesize this into a **mental model** of what the entry object does logically.
4. You are allowed to make educated assumptions, but clearly state them as such.

---

## 💬 Stage 2 – Answer the User Query
After you build a full understanding of the entry object's logic and its dependency tree, proceed to answer the actual user query below.

Make your answer:
- Precise and technical
- Backed by your reasoning from the dependency analysis
- Avoid guessing beyond what is semantically implied

---

## 📦 Dependency Information

```
=== NESTED DEPENDENCY TREE FOR: tms1034.sendung ===
Object Type: TABLE

STRUCTURE:
Columns (5):
  - frankatur unknown
  - firma unknown
  - sendung_tix unknown
  - niederlassung unknown
  - relation unknown

DEPENDENCY TREE (objects this depends on):
tms1034.sendung (TABLE)
tms1034.sendung (TABLE)
└─ tms1034.relation (TABLE)

tms1034.sendung (TABLE)
└─ tms1034.relation (TABLE)
  └─ tms1034.land (TABLE)

tms1034.sendung (TABLE)
└─ tms1034.relation (TABLE)
  └─ tms1034.region (TABLE)

tms1034.sendung (TABLE)
└─ tms1034.frankatur (TABLE)


REVERSE DEPENDENCIES (objects that depend on this):
  ← aggsenpstzustand.updatepst (PROCEDURE)
  ← tms1034.v_zusa (VIEW)
  ← tms1034.v_ta_sen_g (VIEW)
  ← tms1034.v_ta_prot (VIEW)
  ← tms1034.v_sta_abh_unt (VIEW)
  ← tms1034.v_sql_uebern (VIEW)
  ← tms1034.v_sql_del (VIEW)
  ← tms1034.v_sfas_sperre (VIEW)
  ← tms1034.v_sfan (VIEW)
  ← tms1034.v_sfad (VIEW)
  ← tms1034.v_sen_unt (VIEW)
  ← tms1034.v_sen_pst5 (VIEW)
  ← tms1034.v_sen_pst12 (VIEW)
  ← tms1034.v_sen_gew_rrv (VIEW)
  ← tms1034.v_sen_frk_unt (VIEW)
  ← tms1034.v_sen_exp_jmp (VIEW)
  ← tms1034.v_sen8 (VIEW)
  ← tms1034.v_sen5 (VIEW)
  ← tms1034.v_sap_lst_d (VIEW)
  ← tms1034.v_samasen (VIEW)
  ← tms1034.v_rech (VIEW)
  ← tms1034.v_net_ta_sen2 (VIEW)
  ← tms1034.v_net_ta_frb (VIEW)
  ← tms1034.v_mgl_lst_d (VIEW)
  ← tms1034.v_lst_g_rue (VIEW)
  ← tms1034.v_ls_rm_status (VIEW)
  ← tms1034.v_lhm_buch_sen_serf (VIEW)
  ← tms1034.v_lhm_buch_sen_rk (VIEW)
  ← tms1034.v_lhm_buch_sen_fv (VIEW)
  ← tms1034.v_lhm_buch (VIEW)
  ← tms1034.v_kvn_sen_pst (VIEW)
  ← tms1034.v_kvn_fak_sen (VIEW)
  ← tms1034.v_kvn_fak_frk_lkw (VIEW)
  ← tms1034.v_kvn_abh_sen (VIEW)
  ← tms1034.v_kdfibu_sen (VIEW)
  ← tms1034.v_kdfibu_refsen (VIEW)
  ← tms1034.v_frk_gefgut2 (VIEW)
  ← tms1034.v_fak_sen_lst_g (VIEW)
  ← tms1034.v_fak_sen000 (VIEW)
  ← tms1034.v_fak_rrv_bo_e (VIEW)
  ← tms1034.v_fak_rrv_bo_a (VIEW)
  ← tms1034.v_fak_ea2 (VIEW)
  ← tms1034.v_eigenlabel (VIEW)
  ← tms1034.v_ds_sen_ls_se (VIEW)
  ← tms1034.v_ds_sen_ls_sa (VIEW)
  ← tms1034.v_csm_sen (VIEW)
  ← tms1034.v_bse (VIEW)
  ← tms1034.v_bo_dir_sen (VIEW)
  ← tms1034.v_agg_sen_pst_zustand (VIEW)
  ← tms1034.v_abs_pst (VIEW)
  ← tms1034.v_abh_lkw_mde (VIEW)
  ← tms1034.v_dis_transportorder_filter (VIEW)
  ← tms1034.v_sen9 (VIEW)
  ← tms1034.v_dis_shipment (VIEW)
  ← tms1034.v_sen_stempel (VIEW)
  ← tms1034.v_dis_transportorder_pickupplanning (VIEW)
  ← tms1034.v_dis_to_tourpoint (VIEW)
  ← tms1034.v_dis_transportorder_count (VIEW)
  ← tms1034.v_esb_stock (VIEW)
  ← tms1034.v_csm_lhm_buch (VIEW)
  ← tms1034.v_frk_unt4 (VIEW)
  ← tms1034.v_lagerbuch (VIEW)
  ← tms1034.v_net_re_ta_export (VIEW)
  ← tms1034.v_net_ta_unt (VIEW)
  ← tms1034.v_net_ta_dok (VIEW)
  ← tms1034.v_net_ta_intern3 (VIEW)
  ← tms1034.v_net_ta_lst_erlart_nfak (VIEW)
  ← tms1034.v_tms_sen_fak_sel (VIEW)
  ← tms1034.v_net_ta_intern (VIEW)
  ← tms1034.v_net_ta_sen (VIEW)
  ← tms1034.v_sen_ls (VIEW)
  ← tms1034.v_sen_ep2 (VIEW)
  ← tms1034.v_agg_ta_lad_sen_pst_mp4 (VIEW)
  ← tms1034.v_auy (VIEW)
  ← tms1034.v_com_ta_lad (VIEW)
  ← tms1034.v_com_ta_lad_activities (VIEW)
  ← tms1034.v_com_ta3 (VIEW)
  ← tms1034.v_emp_sen_sum (VIEW)
  ← tms1034.v_emp_sen_sum2 (VIEW)
  ← tms1034.v_emp_sen_sum3 (VIEW)
  ← tms1034.v_kvn_pers_loadplace_cons_mp7 (VIEW)
  ← tms1034.v_kvn_sen (VIEW)
  ← tms1034.v_kvn_ta_lad_dis (VIEW)
  ← tms1034.v_net_rl_ta (VIEW)
  ← tms1034.v_net_ta_disp_lite (VIEW)
  ← tms1034.v_net_ta_lst_nfak (VIEW)
  ← tms1034.v_net_ta_lst_nfak2 (VIEW)
  ← tms1034.v_net_ta_tb_aufw2 (VIEW)
  ← tms1034.v_net_ta_tb_aufw (VIEW)
  ← tms1034.v_rel_sen_sum (VIEW)
  ← tms1034.v_rel_sen_sum2 (VIEW)
  ← tms1034.v_rel_sen_sum3 (VIEW)
  ← tms1034.v_sen4 (VIEW)
  ← tms1034.v_ta_dashboard_mp4 (VIEW)
  ← tms1034.v_ta_ll_sen (VIEW)
  ← tms1034.v_ta_red_frv (VIEW)
  ← tms1034.v_ta_red_rv (VIEW)
  ← tms1034.v_ta_sen5 (VIEW)
  ← tms1034.v_ta_zuord (VIEW)
  ← tms1034.v_ta_nvpool (VIEW)
  ← tms1034.v_com_ta_pst_hst (VIEW)
  ← tms1034.v_ta1 (VIEW)
  ← tms1034.v_ta2 (VIEW)
  ← tms1034.v_tms_fak_ta_sen (VIEW)
  ← tms1034.v_top_loadinglist (VIEW)
  ← tms1034.v_wa_ist_buch (VIEW)
  ← tms1034.v_net_ta_aufw (VIEW)
  ← tms1034.v_net_re_ta2 (VIEW)
  ← tms1034.v_esb_measuringpoint5delay (VIEW)
  ← tms1034.v_esb_measuringpoint5detail (VIEW)
  ← tms1034.v_esb_unloadingreport (VIEW)
  ← tms1034.v_esb_sen_on_rk (VIEW)
  ← tms1034.v_esb_sen_on_ll (VIEW)
  ← tms1034.v_sen_sachbearb (VIEW)
  ← tms1034.v_sen_lst_g (VIEW)
  ← tms1034.v_sen_hst_eb_mde (VIEW)
  ← tms1034.v_sen_exp_prioavisemail (VIEW)
  ← tms1034.v_sen_ep1 (VIEW)
  ← tms1034.v_sen3 (VIEW)
  ← tms1034.v_sdskdv01 (VIEW)
  ← tms1034.v_res2_hst (VIEW)
  ← tms1034.v_kvn_ta_lkw_eintreff_sa (VIEW)
  ← tms1034.v_fak_sen2 (VIEW)
  ← tms1034.v_fak_sen (VIEW)
  ← tms1034.v_tour_wl (VIEW)
  ← tms1034.v_ta_nvpool_lad (VIEW)
  ← tms1034.v_ta_lad_sen_sum (VIEW)
  ← tms1034.v_sen_status2 (VIEW)
  ← tms1034.v_sen_status (VIEW)
  ← tms1034.v_sen_pst4 (VIEW)
  ← tms1034.v_sen_pruef (VIEW)
  ← tms1034.v_sen_prot_pst (VIEW)
  ← tms1034.v_sen_prot (VIEW)
  ← tms1034.v_sen_mde (VIEW)
  ← tms1034.v_sen_lst_g_spv (VIEW)
  ← tms1034.v_sen_frk_mde (VIEW)
  ← tms1034.v_sen_exp_tt_partner (VIEW)
  ← tms1034.v_sen_exp_transthermos (VIEW)
  ← tms1034.v_sen_exp_mp4_200430 (VIEW)
  ← tms1034.v_sen_exp_avisemail (VIEW)
  ← tms1034.v_sen_ea6 (VIEW)
  ← tms1034.v_sen_e0 (VIEW)
  ← tms1034.v_sen_dispo_sa2 (VIEW)
  ← tms1034.v_sen_dispo_sa1 (VIEW)
  ← tms1034.v_sen_a0 (VIEW)
  ← tms1034.v_sen6 (VIEW)
  ← tms1034.v_sen2 (VIEW)
  ← tms1034.v_sen (VIEW)
  ← tms1034.v_sdzse (VIEW)
  ← tms1034.v_rk_info (VIEW)
  ← tms1034.v_rel_mp4 (VIEW)
  ← tms1034.v_ta_sen (VIEW)
  ← tms1034.v_ta_lad_sen_pst_mp4_2 (VIEW)
  ← tms1034.v_pers_mobil_tel (VIEW)
  ← tms1034.v_net_ta_iv_tpl (VIEW)
  ← tms1034.v_net_rl_ta_sen (VIEW)
  ← tms1034.v_net_re_ta_erlart (VIEW)
  ← tms1034.v_net_sen_rm (VIEW)
  ← tms1034.v_net_ta_erl (VIEW)
  ← tms1034.v_net_sen_tpl2 (VIEW)
  ← tms1034.v_net_sen_ndis (VIEW)
  ← tms1034.v_net_sen_intern2 (VIEW)
  ← tms1034.v_net_sen_dok (VIEW)
  ← tms1034.v_net_re_bee (VIEW)
  ← tms1034.v_mgl_lhm_sap (VIEW)
  ← tms1034.v_la_w01 (VIEW)
  ← tms1034.v_la_pos_artikel2 (VIEW)
  ← tms1034.v_la_pos3 (VIEW)
  ← tms1034.v_kvn_senzus (VIEW)
  ← tms1034.v_kvn_senabh (VIEW)
  ← tms1034.v_kvn_auto_bo (VIEW)
  ← tms1034.v_kpiu_scanpfl_sen_1 (VIEW)
  ← tms1034.v_kpiu_scanpfl_sen (VIEW)
  ← tms1034.v_kpiu_nichtscanpfl_sen_1 (VIEW)
  ← tms1034.v_kpiu_nichtscanpfl_sen (VIEW)
  ← tms1034.v_kombisen (VIEW)
  ← tms1034.v_lhm_buch_sen_abs (VIEW)
  ← tms1034.v_frk_sen_pst_wek (VIEW)
  ← tms1034.v_frk_sen_pst_hst_mde (VIEW)
  ← tms1034.v_frk_sen_pst4 (VIEW)
  ← tms1034.v_frk_sen3 (VIEW)
  ← tms1034.v_frk_sen2 (VIEW)
  ← tms1034.v_frk_sen (VIEW)
  ← tms1034.v_frk_pst_nosen (VIEW)
  ← tms1034.v_frk4 (VIEW)
  ← tms1034.v_fak_sen_lst_g_rrv (VIEW)
  ← tms1034.v_fak_lst_g_rrv (VIEW)
  ← tms1034.v_frk_gefgut (VIEW)
  ← tms1034.v_fak_lst_b_g (VIEW)
  ← tms1034.v_fak_ea_ph (VIEW)
  ← tms1034.v_fak_ea1 (VIEW)
  ← tms1034.v_fak_ea (VIEW)
  ← tms1034.v_eu1_sen (VIEW)
  ← tms1034.v_com_ta_sen (VIEW)
  ← tms1034.v_com_ta_pst_mp2 (VIEW)
  ← tms1034.v_com_ta_prodgrp (VIEW)
  ← tms1034.v_ta_sen7 (VIEW)
  ← tms1034.v_com_ueberz (VIEW)
  ← tms1034.v_com_masterpst (VIEW)
  ← tms1034.v_com_pst (VIEW)
  ← tms1034.v_com_sen2 (VIEW)
  ← tms1034.v_com_sen (VIEW)
  ← tms1034.v_com_nl (VIEW)
  ← tms1034.v_auq_pst (VIEW)
  ← tms1034.v_ta_prodgrp (VIEW)
  ← tms1034.v_net_sen_lst_g (VIEW)
  ← tms1034.v_la2 (VIEW)
  ← tms1034.v_frk_sen_pst_agg_thin (VIEW)
  ← tms1034.v_frk_sen_pst_agg (VIEW)
  ← tms1034.v_ta_prodgrp_old (VIEW)
  ← tms1034.v_ta_zusa_sen (VIEW)
  ← tms1034.v_abs_pst2 (VIEW)
  ← tms1034.v_abs_sen_pst2 (VIEW)
  ← tms1034.v_abs_sen_pst (VIEW)
  ← tms1034.v_abs_pst_mp (VIEW)
  ← tms1034.v_net_sen_intern (VIEW)
  ← tms1034.v_frk_sen_pst_thin (VIEW)
  ← tms1034.v_frk_sen_pst (VIEW)
  ← tms1034.v_sen_pst15 (VIEW)
  ← tms1034.v_sen_pst3 (VIEW)
  ← tms1034.v_net_ta_abh3 (VIEW)
  ← tms1034.v_ta_tour_abh (VIEW)
  ← tms1034.v_ta_abh (VIEW)
  ← tms1034.v_ta_abh3 (VIEW)
  ← tms1034.v_ta_abh2 (VIEW)
  ← tms1034.v_net_ta_abh2 (VIEW)
  ← tms1034.v_ta_sen6 (VIEW)
  ← tms1034.v_sen_dispo_nv_thin (VIEW)
  ← tms1034.v_sen_dispo_nv (VIEW)
  ← tms1034.v_sen7_thin (VIEW)
  ← tms1034.v_sen7 (VIEW)
  ← tms1034.v_la (VIEW)
  ← tms1034.v_kdfibu_pos_thin (VIEW)
  ← tms1034.v_kdfibu_pos (VIEW)
  ← tms1034.v_esb_ciconsignmentundelivered (VIEW)
  ← tms1034.v_esb_sen_hst (VIEW)
  ← tms1034.v_esb_faklstg (VIEW)
  ← tms1034.v_esb_del_ciconsignundelivered (VIEW)
  ← tms1034.v_sen_maut (VIEW)
  ← tms1034.v_sama_sen_lst_g (VIEW)
  ← tms1034.v_csm_sen_tourpunkt (VIEW)
  ← tms1034.v_ta3 (VIEW)
  ← tms1034.v_ta_lad (VIEW)
  ← tms1034.v_kvn_ta_lad_abf (VIEW)
  ← tms1034.v_xserver_ta_order (VIEW)
  ← tms1034.v_ta_tour_mp7 (VIEW)
  ← tms1034.v_ta_sen4 (VIEW)
  ← tms1034.v_ta_nofuncs (VIEW)
  ← tms1034.v_ta (VIEW)
  ← tms1034.v_aa (VIEW)
  ← tms1034.v_sen_ep (VIEW)
  ← tms1034.v_sen_lst_b (VIEW)
  ← tms1034.v_ta_sen3 (VIEW)
  ← tms1034.v_net_re_ta (VIEW)
  ← tms1034.v_net_sen_text (VIEW)
  ← pdriverterminal.printtransportorderdocuments (PROCEDURE)
  ← pdriverterminal.gettransportorderinfodto (FUNCTION)
  ← pdriverterminal.getmeasuringpoint (FUNCTION)
  ← udb_xsb.get_rowfra (FUNCTION)
  ← tmsdfvavis.reorgsents (PROCEDURE)
  ← tmsdfvavis.deactivatesents (PROCEDURE)
  ← tmsdfvavis.activateandsendavise (PROCEDURE)
  ← tmsdfvavis.getmetat (FUNCTION)
  ← pdis_transportorderdto.addgoodstypes (PROCEDURE)
  ← pdis_transportorderdto.addfotasklist (PROCEDURE)
  ← pdis_transportorderdto.needsprinting (FUNCTION)
  ← pdis_transportorderdto.getforwardingorderlistdto (FUNCTION)
  ← pdis_transportorderdto.get (FUNCTION)
  ← eb_eu1.setrsen (PROCEDURE)
  ← dbto_pta_rv2_ta.dbto_pta_rv2_ta (FUNCTION)
  ← digilis_queue.writeprintdnevent (PROCEDURE)
  ← digilis_queue.writeconnectstatedelnoteevent (PROCEDURE)
  ← digilis_queue.transfertotms (PROCEDURE)
  ← udb_jmp.exportfile (PROCEDURE)
  ← udb_jmp.export (PROCEDURE)
  ← dbto_pta_rv_ta.dbto_pta_rv_ta (FUNCTION)
  ← reorglib.reorganise_reshst (PROCEDURE)
  ← reorglib.reorganise_la (PROCEDURE)
  ← reorglib.reorganise_aggmp4 (PROCEDURE)
  ← ptavis.createsendung (FUNCTION)
  ← pproduct.sendaus (PROCEDURE)
  ← pproduct.validatedeliverydateexceeded (PROCEDURE)
  ← disp_mde_rk.tauschesendung (PROCEDURE)
  ← disp_mde_rk.stornosendung (PROCEDURE)
  ← disp_mde_rk.stornobeladung (PROCEDURE)
  ← disp_mde_rk.setzerklhm (PROCEDURE)
  ← disp_mde_rk.scansendung (PROCEDURE)
  ← disp_mde_rk.startbeladung (PROCEDURE)
  ← disp_mde_rk.reservieresendung (PROCEDURE)
  ← disp_mde_rk.freigabesendung (PROCEDURE)
  ← disp_mde_rk.erzeugeposition (PROCEDURE)
  ← disp_mde_rk.endebeladung (PROCEDURE)
  ← disp_mde_rk.getrksum (PROCEDURE)
  ← disp_mde_rk.bestimmesendung (PROCEDURE)
  ← disp_mde_rk.scanbarcode (PROCEDURE)
  ← disp_mde_rk.abschlsendung (PROCEDURE)
  ← disp_mde_rk.abschlbeladung (PROCEDURE)
  ← disp_mde_rk.bestabschreib (PROCEDURE)
  ← disp_mde_rk.setsenendebeladung (FUNCTION)
  ← disp_mde_rk.bestimmeseninfo (FUNCTION)
  ← disp_mde_rk.setsenstatusfrompststatus (FUNCTION)
  ← disp_mde_rk.pruefesendung (PROCEDURE)
  ← disp_mde_rk.erzeugenachlieferung (PROCEDURE)
  ← dbto_yardmsg.dbto_yardmsg (FUNCTION)
  ← pta_dashboard_mp4.transmit (PROCEDURE)
  ← pta_dashboard_mp4.readfirstlad (PROCEDURE)
  ← pyard_lib.readfrk (PROCEDURE)
  ← pyard_lib.gettalad (FUNCTION)
  ← pyard_lib.getstatinfo (FUNCTION)
  ← pv_xserver_ta_nvpool_ll_order.get (FUNCTION)
  ← cal_agg.fillaggta (PROCEDURE)
  ← cal_agg.addaggtaladsenpstmp4_plproxy (FUNCTION)
  ← cal_agg_rp.fillaggtaladsenpstmp4 (PROCEDURE)
  ← cal_agg_rp.fillaggta (PROCEDURE)
  ← cal_agg_rp.fillaggcomta2 (PROCEDURE)
  ← pta2.reorg (PROCEDURE)
  ← pta2.pgettalist (PROCEDURE)
  ← pta2.assignfrk (PROCEDURE)
  ← pta2.reassignfrk (PROCEDURE)
  ← pta2.fiswbusedwithtpl (FUNCTION)
  ← pta2.fisuntusedwithtpl (FUNCTION)
  ← pta2.fislkwusedwithtpl (FUNCTION)
  ← pta2.fisfahrerusedwithtpl (FUNCTION)
  ← pta2.fisanhusedwithtpl (FUNCTION)
  ← pv_la3_1.getdata (FUNCTION)
  ← pta_rv.dorv (PROCEDURE)
  ← pta_rv.clearrv (PROCEDURE)
  ← pta_nvpool.splitpoint (PROCEDURE)
  ← pta_nvpool.merge (PROCEDURE)
  ← pta_nvpool.delavis (PROCEDURE)
  ← pta_nvpool.checkeqmavailability (PROCEDURE)
  ← pta_nvpool.addsen (PROCEDURE)
  ← pta_nvpool.acceptplannedll (PROCEDURE)
  ← pta_nvpool.gettsen (FUNCTION)
  ← pta_nvpool.getstatus (FUNCTION)
  ← pta_nvpool.getid (FUNCTION)
  ← sen2.lockrec (FUNCTION)
  ← sen2.getrrvzusaref (FUNCTION)
  ← sen2.setseavisautoend (PROCEDURE)
  ← sen2.getnextfrkposlfdn (FUNCTION)
  ← sen2.assignorigsen (PROCEDURE)
  ← sen2.gettsen_ta_frk_tmpsen (FUNCTION)
  ← dfv_sxt.processkewillhst (PROCEDURE)
  ← dfv_sxt.processkewillbtr (PROCEDURE)
  ← dfv_sat.switchpst2mdepst (PROCEDURE)
  ← dfv_sat.setabhtourdaten (PROCEDURE)
  ← dfv_sat.putstorckpauschale (PROCEDURE)
  ← dfv_sat.processsenlshst (PROCEDURE)
  ← dfv_sat.processsenhst (PROCEDURE)
  ← dfv_sat.processsen (PROCEDURE)
  ← dfv_sat.setabhrollkart (FUNCTION)
  ← dfv_sat.copysenpstlinks (FUNCTION)
  ← dfv_sat.processbosenhst (PROCEDURE)
  ← dfv_set.processsenlstb (PROCEDURE)
  ← dfv_set.processsenlshst (PROCEDURE)
  ← dfv_set.processsenhst (PROCEDURE)
  ← dfv_set.copysenhststatusrak (PROCEDURE)
  ← disp_mde_eb.zuordpstsendung (PROCEDURE)
  ← disp_mde_eb.stornosendung (PROCEDURE)
  ← disp_mde_eb.scansendung (PROCEDURE)
  ← disp_mde_eb.gensenstatus (PROCEDURE)
  ← disp_mde_eb.erzeugeueberzaehligkeit (PROCEDURE)
  ← disp_mde_eb.erzeugenlwlausuz (PROCEDURE)
  ← disp_mde_eb.erzeugenachlieferung (PROCEDURE)
  ← disp_mde_eb.bestsortware (PROCEDURE)
  ← disp_mde_eb.bestseninfo (PROCEDURE)
  ← disp_mde_eb.bestsenentlinfo (PROCEDURE)
  ← disp_mde_eb.bestlhmlkw (PROCEDURE)
  ← disp_mde_eb.abschlsendung (PROCEDURE)
  ← dfv_set.deletesendung (FUNCTION)
  ← disp_mde_eb.abschlnve (PROCEDURE)
  ← disp_mde_eb.abschlueberzaehligkeit (PROCEDURE)
  ← disp_mde_eb.erzeugeposition (PROCEDURE)
  ← ibaf_ta.getupdatetransportorderdto (FUNCTION)
  ← ibaf_ta.getsubcontractordto (FUNCTION)
  ← ibaf_ta.getpaymentdetailsdto (FUNCTION)
  ← ibaf_ta.getaddtransportorderdto (FUNCTION)
  ← paa.setstatus (PROCEDURE)
  ← paa.setberval (PROCEDURE)
  ← paa.resetstatus (PROCEDURE)
  ← paa.put (PROCEDURE)
  ← paa.lockrec (PROCEDURE)
  ← paa.delsplitpst (PROCEDURE)
  ← paa.del (PROCEDURE)
  ← paa.change (PROCEDURE)
  ← paa.canexecute (PROCEDURE)
  ← paa.lockrec (FUNCTION)
  ← paa.getstatust (FUNCTION)
  ← paa.copyfromtpl (FUNCTION)
  ← paa.copyastpl (FUNCTION)
  ← paa.copy (FUNCTION)
  ← paa.fgetid (FUNCTION)
  ← trig_v_aa.ioiud (FUNCTION)
  ← prk.remsen2 (PROCEDURE)
  ← prk.movesen2 (PROCEDURE)
  ← prk.del (PROCEDURE)
  ← prk.addsen2 (PROCEDURE)
  ← prk.addfrk (PROCEDURE)
  ← prk.actualize (PROCEDURE)
  ← prk.gettsen (FUNCTION)
  ← prk.getinsertpos (FUNCTION)
  ← ppersladort.assign (PROCEDURE)
  ← eb.modmp5bymp7 (PROCEDURE)
  ← eb.storno (PROCEDURE)
  ← eb.setsenentl (PROCEDURE)
  ← eb.put (PROCEDURE)
  ← pll.remsen2 (PROCEDURE)
  ← pll.movesen2 (PROCEDURE)
  ← pll.delavis (PROCEDURE)
  ← pll.del (PROCEDURE)
  ← pll.canexecute (PROCEDURE)
  ← pll.addsen (PROCEDURE)
  ← pll.actualizeunteqmfromta (PROCEDURE)
  ← pll.actualizesen (PROCEDURE)
  ← pll.actualize (PROCEDURE)
  ← pll.getinsertpos (FUNCTION)
  ← calnet_ta.split (PROCEDURE)
  ← calnet_ta.setsenlstd (PROCEDURE)
  ← calnet_ta.setlstd (PROCEDURE)
  ← calnet_ta.setdone (PROCEDURE)
  ← calnet_ta.remsen (PROCEDURE)
  ← calnet_ta.merge (PROCEDURE)
  ← calnet_ta.initsession (PROCEDURE)
  ← calnet_ta.dispatch (PROCEDURE)
  ← calnet_ta.cancelsendispatch (PROCEDURE)
  ← calnet_ta.canceldispatch (PROCEDURE)
  ← calnet_ta.delavis (PROCEDURE)
  ← calnet_ta.hasavisonly (FUNCTION)
  ← calnet_ta.getunfaufwg (FUNCTION)
  ← calnet_ta.getrefstartort_sav2 (FUNCTION)
  ← calnet_ta.getrefstartort_sav (FUNCTION)
  ← calnet_ta.getrefstartort_old (FUNCTION)
  ← calnet_ta.getrefstartort (FUNCTION)
  ← calnet_ta.getredaufwg (FUNCTION)
  ← calnet_ta.getexttan (FUNCTION)
  ← calnet_ta.geteigenaufwg (FUNCTION)
  ← calnet_ta.getaufwg (FUNCTION)
  ← calnet_ta.getabhlfdn (FUNCTION)
  ← calnet_ta.getbarcode (PROCEDURE)
  ← calnet_ta.geterlg (FUNCTION)
  ← tms2esb.ciconsignmentundelivered2csm (PROCEDURE)
  ← tms2esb.auftragzustandmeldung2oms (PROCEDURE)
  ← ptop_loadinglistdto.getdifferentstartendtourpoint (PROCEDURE)
  ← pll.gettsen (FUNCTION)
  ← ptop_loadinglistdto.gettourelementlistdto (FUNCTION)
  ← ptop_loadinglistdto.getrequiredvehicleequipmentdto (FUNCTION)
  ← ptop_loadinglistdto.getproductgroupsquantitiesdto (FUNCTION)
  ← ptop_loadinglistdto.getlocationlistdto (FUNCTION)
  ← ptop_loadinglistdto.get (FUNCTION)
  ← ptop_loadinglistdto.getorderlistdto (FUNCTION)
  ← pfrk.resetspaetbelad (PROCEDURE)
  ← pfrk.refresh (PROCEDURE)
  ← pfrk.korrfrksen (PROCEDURE)
  ← pfrk.getebosum (PROCEDURE)
  ← pfrk.dispatch_dispatchbosen (PROCEDURE)
  ← pfrk.del (PROCEDURE)
  ← pfrk.copy2dfv (PROCEDURE)
  ← pfrk.isbocomplete (FUNCTION)
  ← pfrk.hastksen (FUNCTION)
  ← pfrk.gettyp (FUNCTION)
  ← pfrk.gettprodgrpk (FUNCTION)
  ← pfrk.getsen_c3 (FUNCTION)
  ← pfrk.getsen_c2 (FUNCTION)
  ← pfrk.getsen_c (FUNCTION)
  ← pfrk.getfirsttksen (FUNCTION)
  ← pfrk.dispatch_getavibo2dfvtixlist (FUNCTION)
  ← pfrk.checkseneqm (FUNCTION)
  ← pfrk.setdispatchend (PROCEDURE)
  ← pfrk.setmp4senstatus (PROCEDURE)
  ← disp_mde_rk.korrsendung (PROCEDURE)
  ← pfrk.dispatch_getbosenpos (FUNCTION)
  ← disp_speich.rollkarte (PROCEDURE)
  ← disp_speich.ladeliste (PROCEDURE)
  ← disp_speich.sendsort (FUNCTION)
  ← disp_speich.bordero (PROCEDURE)
  ← calnet_senhst.pput (PROCEDURE)
  ← calnet_sen.setsys (PROCEDURE)
  ← calnet_sen.paddunteqm (PROCEDURE)
  ← calnet_sen.setlstd (PROCEDURE)
  ← calnet_sen.setstatusactive (PROCEDURE)
  ← calnet_sen.del (PROCEDURE)
  ← calnet_sen.copyfromtpl (PROCEDURE)
  ← calnet_sen.checkcomplete (PROCEDURE)
  ← calnet_sen.getta (FUNCTION)
  ← calnet_sen.actualizetastatistics (PROCEDURE)
  ← calnet_sen.getbarcode (FUNCTION)
  ← calnet_sen.getbarcode (PROCEDURE)
  ← calnet_sen.getneterlg (FUNCTION)
  ← calnet_sen.getnetaufwg (FUNCTION)
  ← calnet_sen.geterlg (FUNCTION)
  ← calnet_sen.checkcomplete (FUNCTION)
  ← calnet_sen.getaufwg (FUNCTION)
  ← plapos.put (PROCEDURE)
  ← pla.setlag (PROCEDURE)
  ← pla.lockrec (PROCEDURE)
  ← pla.execsub (PROCEDURE)
  ← pla.del (PROCEDURE)
  ← pla.checkpersartikel (PROCEDURE)
  ← pla.checkloadfitting (PROCEDURE)
  ← pla.pcanexecute (PROCEDURE)
  ← pla.addoutboundpos (PROCEDURE)
  ← pla.actualpos (PROCEDURE)
  ← pla.lockrec (FUNCTION)
  ← pla.isla (FUNCTION)
  ← pla.hasstatus2neu (FUNCTION)
  ← pla.hasstatus2 (FUNCTION)
  ← pla.hasstatus (FUNCTION)
  ← pla.getid (FUNCTION)
  ← pla.copy (FUNCTION)
  ← disp_mde_common.setmandatoryscanmp2 (PROCEDURE)
  ← disp_mde_common.resetmandatoryscanmp2 (PROCEDURE)
  ← disp_mde_common.drucknve (PROCEDURE)
  ← disp_mde_common.createueberzaehligkeit (PROCEDURE)
  ← disp_mde_common.addueberzaehligkeitpos (PROCEDURE)
  ← disp_mde_common.addrefsentometat (PROCEDURE)
  ← disp_mde_common.addpsttosenls (PROCEDURE)
  ← disp_mde_common.addpsttosen (PROCEDURE)
  ← disp_mde_common.getrefsenmetat (FUNCTION)
  ← disp_mde_common.getfirstsuppldelnondisplk (FUNCTION)
  ← disp_mde_common.getfirstsuppldelnondispatched (FUNCTION)
  ← disp_mde_common.getconstypefrommasterpacktix (FUNCTION)
  ← disp_mde_common.getbarcode (FUNCTION)
  ← disp_mde_common.createmasternve (FUNCTION)
  ← disp_mde_common.addpststatusmp6 (FUNCTION)
  ← disp_mde_common.addpststatusmp4 (FUNCTION)
  ← disp_mde_common.addpststatusmp3 (FUNCTION)
  ← disp_mde_common.checkpstprocess (FUNCTION)
  ← reshst.copyreshstzus2dfv (PROCEDURE)
  ← reshst.movedefault (PROCEDURE)
  ← reshst.copy (FUNCTION)
  ← reshst.copy (PROCEDURE)
  ← reshst.getmautentf (FUNCTION)
  ← reshst.getentf (FUNCTION)
  ← reshst.getmaxprodinternk (FUNCTION)
  ← reshst.getprio (FUNCTION)
  ← reshst.addort (FUNCTION)
  ← pwa.setdone (PROCEDURE)
  ← pwa.lockrec (PROCEDURE)
  ← pwa.exec (PROCEDURE)
  ← pwa.del (PROCEDURE)
  ← pwa.actualpos (PROCEDURE)
  ← pwa.lockrec (FUNCTION)
  ← pwa.hasstatus (FUNCTION)
  ← pwa.getlade (FUNCTION)
  ← pwa.settb (PROCEDURE)
  ← plagerbuch.putlstg (PROCEDURE)
  ← plagerbuch.addaa (PROCEDURE)
  ← plagerbuch.getcondstoreintervals (FUNCTION)
  ← formgen.getdokta (FUNCTION)
  ← pwa.fgetid (FUNCTION)
  ← dbto_frkinfo.dbto_frkinfo (FUNCTION)
  ← pta.settourstplcgewfrompstc (PROCEDURE)
  ← pta.setzsenstplcgewfrompstc (PROCEDURE)
  ← pta.inittourstplcgew (PROCEDURE)
  ← pta.wlsen (PROCEDURE)
  ← pta.undowlsen (PROCEDURE)
  ← pta.stornobelad (PROCEDURE)
  ← pta.setstatus (PROCEDURE)
  ← pta.setsenvkstrom (PROCEDURE)
  ← pta.setsenstpl_c (PROCEDURE)
  ← pta.setsengew (PROCEDURE)
  ← pta.setrta_status (PROCEDURE)
  ← pta.setrel (PROCEDURE)
  ← pta.setmodus2 (PROCEDURE)
  ← pta.setrv_ta (PROCEDURE)
  ← pta.setlstfakfreigabe2 (PROCEDURE)
  ← pta.send2mde (PROCEDURE)
  ← pta.remzusasenfromll (PROCEDURE)
  ← pta.remsenfromll (PROCEDURE)
  ← pta.remsenactualizezusaprod (PROCEDURE)
  ← pta.remort (PROCEDURE)
  ← pta.resetstatus (PROCEDURE)
  ← pta.remsen (PROCEDURE)
  ← pta.reassignsen (PROCEDURE)
  ← pta.readlstdata (PROCEDURE)
  ← pta.pgetbarcode (PROCEDURE)
  ← pta.paddsen (PROCEDURE)
  ← pta.pgetwlrel (PROCEDURE)
  ← pta.moveort (PROCEDURE)
  ← pta.initdispoplan (PROCEDURE)
  ← pta.getzusa (PROCEDURE)
  ← pta.lockrec (PROCEDURE)
  ← pta.gettalist (PROCEDURE)
  ← pta.getsenressource (PROCEDURE)
  ← pta.getortmaxprioprod (PROCEDURE)
  ← pta.getcomplete (PROCEDURE)
  ← pta.getabfzsenlist (PROCEDURE)
  ← pta.fillautosgsengew2 (PROCEDURE)
  ← pta.execsub (PROCEDURE)
  ← pta.del (PROCEDURE)
  ← pta.clearautosgsengew2 (PROCEDURE)
  ← pta.checkseneqm (PROCEDURE)
  ← pta.canexecutesub (PROCEDURE)
  ← pta.assignsen2 (PROCEDURE)
  ← pta.addzusa (PROCEDURE)
  ← pta.addzusa (FUNCTION)
  ← pta.checkbgvk (PROCEDURE)
  ← pta.addsentoll (PROCEDURE)
  ← pta.exec (PROCEDURE)
  ← pta.addsen8 (PROCEDURE)
  ← pta.addsen7 (PROCEDURE)
  ← pta.preassignfrk (PROCEDURE)
  ← pta.addsen6 (PROCEDURE)
  ← pta.addsen5 (PROCEDURE)
  ← pta.addsen4 (PROCEDURE)
  ← pta.addsen (PROCEDURE)
  ← pta.actualizesubtatour (PROCEDURE)
  ← pta.actualizefromsen (PROCEDURE)
  ← pta.addpstmp4 (PROCEDURE)
  ← pta.getortbodenstpl_c (FUNCTION)
  ← pta.new (FUNCTION)
  ← pta.ista (FUNCTION)
  ← pta.isnv (FUNCTION)
  ← pta.isfv (FUNCTION)
  ← pta.hasta2 (FUNCTION)
  ← pta.hasstatus (FUNCTION)
  ← pta.hasabl (FUNCTION)
  ← pta.gettprodgrpk (FUNCTION)
  ← pta.gettix (FUNCTION)
  ← pta.gettatixfrombarcode (FUNCTION)
  ← pta.getsenstatus (FUNCTION)
  ← pta.getsenid (FUNCTION)
  ← pta.isnvpool (FUNCTION)
  ← pta.getzsenbeladpstc (FUNCTION)
  ← pta.getprodgrpinfo (FUNCTION)
  ← pta.getorttlhm2_getsenmdelhm (FUNCTION)
  ← pta.hassen (FUNCTION)
  ← pta.getorttlhm2_getmasterpstmdelhm (FUNCTION)
  ← pta.getorttlhm2_getladvorgangmdelhm (FUNCTION)
  ← pta.getn (FUNCTION)
  ← pta.getivtyp (FUNCTION)
  ← pta.getid (FUNCTION)
  ← pta.getfvbeladname (FUNCTION)
  ← pta.getentlstatus (FUNCTION)
  ← pta.getdfltcopyfrv (FUNCTION)
  ← pta.fisalreadyused (FUNCTION)
  ← pta.geteqmid (FUNCTION)
  ← pta.fgetortlstd (FUNCTION)
  ← pta.fgetkammerinfo2 (FUNCTION)
  ← pta.fhasta3 (FUNCTION)
  ← pta.getwlfrkrel (FUNCTION)
  ← pta.addll (FUNCTION)
  ← pta.addfrk (FUNCTION)
  ← pta.canaddautobegegnungort (FUNCTION)
  ← pta.rempstmp4 (PROCEDURE)
  ← pta.getorttlhm2 (FUNCTION)
  ← pta.copy (PROCEDURE)
  ← pta.modfrk (PROCEDURE)
  ← tmsdfv.setsents (PROCEDURE)
  ← tmsdfv.putvorlaeufigeb (PROCEDURE)
  ← tmsdfv.putpststatus (PROCEDURE)
  ← tmsdfv.putfrkstatus (PROCEDURE)
  ← tmsdfv.copyvks99sen2zdb (PROCEDURE)
  ← tmsdfv.copyta (FUNCTION)
  ← tmsdfv.copynvsen2sisy (FUNCTION)
  ← tmsdfv.copynvsen2sisy (PROCEDURE)
  ← tmsdfv.copymanualabh2zdb (PROCEDURE)
  ← tmsdfv.copyboavis2dfv (PROCEDURE)
  ← tmsdfv.copyfrk (PROCEDURE)
  ← tmsdfv.getrkhd (FUNCTION)
  ← tmsdfv.getbohd (FUNCTION)
  ← tmsdfv.copymanualabh2zdb (FUNCTION)
  ← tmsdfv.copyta (PROCEDURE)
  ← tmsdfv.copydfv (PROCEDURE)
  ← pta_rv2.getorgtix (FUNCTION)
  ← pta_rv2.getladgew (FUNCTION)
  ← pta_rv2.dorv (PROCEDURE)
  ← lst2zdb.createstatus32forlst (PROCEDURE)
  ← lst2zdb.changedsen2dfv (PROCEDURE)
  ← lst2zdb.tmsauditreorg (PROCEDURE)
  ← lst2zdb.tmsauditreorg (FUNCTION)
  ← senlst.setunfreiksp (PROCEDURE)
  ← senlst.setsptsperre (PROCEDURE)
  ← senlst.setrrvsperre (PROCEDURE)
  ← senlst.setrrvabrd (PROCEDURE)
  ← senlst.setfakangaben (PROCEDURE)
  ← senlst.festschreibunglstspt (PROCEDURE)
  ← senlst.festschreibunglstrrv (PROCEDURE)
  ← senlst.copyborrv2dfv (PROCEDURE)
  ← senlst.copy2dfv (PROCEDURE)
  ← senlst.setwnnksp (PROCEDURE)
  ← senlst.copyfromdfv (PROCEDURE)
  ← senlst.copysenlstb2dfv (PROCEDURE)
  ← rorgsen.reorgtranaufgraf (PROCEDURE)
  ← rorgsen.reorgentlber (PROCEDURE)
  ← rorgsen.reorgeingbord (PROCEDURE)
  ← rorgsen.delzusasen (PROCEDURE)
  ← rorgsen.rorg_sendung (PROCEDURE)
  ← pfvsen_rv.pdorv (PROCEDURE)
  ← pfvsen_rv.dorv (PROCEDURE)
  ← pfak.getufvkst (PROCEDURE)
  ← pfak.checkrrvborderos (PROCEDURE)
  ← pfak.getsammelauftid (FUNCTION)
  ← disp_mde_remote.bestimmeueberz (FUNCTION)
  ← disp_mde_ah.scanbarcode (PROCEDURE)
  ← disp_mde_ah.endeentladung (PROCEDURE)
  ← disp_mde_ah.abschlnve (PROCEDURE)
  ← dbto_tour.dbto_tour (FUNCTION)
  ← dbto_mde_sortierwarefinishinfo.execute (PROCEDURE)
  ← ppst.getallsenfrompstn (PROCEDURE)
  ← ppst.getallsenfrompstn (FUNCTION)
  ← ppst.getallpstfrompstn (PROCEDURE)
  ← ppst.getallpstfrompstn (FUNCTION)
  ← ppst.getallmasterpstfrompstn (PROCEDURE)
  ← ppst.getallmasterpstfrompstn (FUNCTION)
  ← ppst.connectsenlabels (PROCEDURE)
  ← ppst.addpstzuord (PROCEDURE)
  ← ppst.isinzyklus (FUNCTION)
  ← ppst.isinlifezyklus (FUNCTION)
  ← ppst.hassen2 (FUNCTION)
  ← ppst.getsenfrompsttix (FUNCTION)
  ← ppst.getsen (FUNCTION)
  ← ppst.addpstzuord (FUNCTION)
  ← pta_entl.setsenentlend (PROCEDURE)
  ← pta_entl.setglnentlvorlend (PROCEDURE)
  ← pta_entl.setglnentlend (PROCEDURE)
  ← pta_entl.setglnau7printed (PROCEDURE)
  ← pta_entl.setabsentlend (PROCEDURE)
  ← pta_entl.setabsau7printed (PROCEDURE)
  ← pta_entl.putdfvglnunt (PROCEDURE)
  ← pta_entl.putdfvabsunt (PROCEDURE)
  ← pta_entl.setabsentlvorlend (PROCEDURE)
  ← pta_entl.hasglnstatus (PROCEDURE)
  ← pta_entl.connectglnlabels (PROCEDURE)
  ← pta_entl.connectabslabels (PROCEDURE)
  ← pta_entl.getsenentllhmdiff (FUNCTION)
  ← pta_entl.getabssenentllhm (FUNCTION)
  ← pta_entl.fgetglnsenentllhm (FUNCTION)
  ← pta_entl.hasabsstatus (PROCEDURE)
  ← plagerplatzvorgang.setend (PROCEDURE)
  ← plagerplatzvorgang.fgetinfo (FUNCTION)
  ← plagerplatzvorgang.getlakonotifytemplate (FUNCTION)
  ← disp_mde_bo.zuordsenta (PROCEDURE)
  ← disp_mde_bo.test_scannve (PROCEDURE)
  ← disp_mde_bo.startbeladungeqm (PROCEDURE)
  ← disp_mde_bo.startbeladungentl (PROCEDURE)
  ← disp_mde_bo.schreibeeqmschaden (PROCEDURE)
  ← disp_mde_bo.endebeladung (PROCEDURE)
  ← disp_mde_bo.startbeladungtor (PROCEDURE)
  ← disp_mde_bo.aenderbeladungtor (PROCEDURE)
  ← disp_mde_bo.abschlbeladevorgang (PROCEDURE)
  ← disp_mde_bo.oeffnebeladevorgang (FUNCTION)
  ← sen_remote.genueberzaehligkeit (PROCEDURE)
  ← sen_remote.genfehlleitung (PROCEDURE)
  ← pzustand.setsenzustand (PROCEDURE)
  ← pzustand.setpstzustand (PROCEDURE)
  ← gft_nl.writescanstatus (PROCEDURE)
  ← gft_nl.checksentix (FUNCTION)
  ← gft_nl.checksenbarcode (FUNCTION)
  ← gft_nl.checkalternative (FUNCTION)
  ← gft_nl.getsendata (PROCEDURE)
  ← sen_hst_eb_mde_v.getmdeentlstand (FUNCTION)
  ← psthst.copy2dfv (PROCEDURE)
  ← ls_digilis.setsendlssettings (PROCEDURE)
  ← ls_digilis.getsenlsperssettings (PROCEDURE)
  ← ls_digilis.getlslsperssettings (PROCEDURE)
  ← frk_mde_v.bestrksenc (FUNCTION)
  ← frk_mde_v.bestrkllsenc (FUNCTION)
  ← digilis_lib.getsenaendxml (FUNCTION)
  ← digilis_lib.getsenloeschxml (FUNCTION)
  ← digilis_remote.haspdf (PROCEDURE)
  ← dbto_tour_order.dbto_tour_order (FUNCTION)
  ← dbto_prodvld_order.dbto_prodvld_order (FUNCTION)
  ← ptour.get (PROCEDURE)
  ← disp_ende.pquit (PROCEDURE)
  ← disp_ende.cleanup (PROCEDURE)
  ← dfvfrk.fillpkgtables (PROCEDURE)
  ← testausgabe.test11 (PROCEDURE)
  ← testausgabe.test09 (PROCEDURE)
  ← testausgabe.test05 (PROCEDURE)
  ← testausgabe.test08 (PROCEDURE)
  ← senref.getreftix (FUNCTION)
  ← senhst.putfrk (PROCEDURE)
  ← sen.triggersetendgueltigse (PROCEDURE)
  ← sen.settour (PROCEDURE)
  ← sen.setprintstatus (PROCEDURE)
  ← sen.setkombisenzus (PROCEDURE)
  ← sen.setkombisenref (PROCEDURE)
  ← sen.setcache (PROCEDURE)
  ← sen.readprintcontext (PROCEDURE)
  ← sen.raiseexception (PROCEDURE)
  ← sen.pgetbarcode (PROCEDURE)
  ← sen.pcanexecute (PROCEDURE)
  ← sen.lockrec (PROCEDURE)
  ← sen.kopiereauftr (PROCEDURE)
  ← sen.getsenlistdependsonmasternve (PROCEDURE)
  ← sen.getsenausbarcode (PROCEDURE)
  ← sen.getsenartintk (PROCEDURE)
  ← sen.getbeladlhm4 (PROCEDURE)
  ← sen.genruecklieferung (PROCEDURE)
  ← sen.gennachlieferung (PROCEDURE)
  ← sen.del (PROCEDURE)
  ← sen.genueberzaehligkeit (PROCEDURE)
  ← sen.copyfromtpl (PROCEDURE)
  ← sen.copyastpl (PROCEDURE)
  ← sen.comparesen (PROCEDURE)
  ← sen.checkcomplete (PROCEDURE)
  ← sen.checkcomplete (FUNCTION)
  ← sen.genweiterleitung (PROCEDURE)
  ← sen.archivepod (PROCEDURE)
  ← sen.actualpossum (PROCEDURE)
  ← sen.isvorlaeufig (FUNCTION)
  ← sen.istrmstatusnzanzf (FUNCTION)
  ← sen.istretoure (FUNCTION)
  ← sen.istfrachtberechnet (FUNCTION)
  ← sen.istabgefertigt (FUNCTION)
  ← sen.hasavailableconnectedsen (FUNCTION)
  ← sen.getsenxml (FUNCTION)
  ← sen.getschedule (FUNCTION)
  ← sen.put (PROCEDURE)
  ← sen.getnldaten (FUNCTION)
  ← sen.getmetaid (FUNCTION)
  ← sen.getlspersk (FUNCTION)
  ← sen.getkombisenzus (FUNCTION)
  ← sen.getkombisenref (FUNCTION)
  ← sen.getkombisenlist (FUNCTION)
  ← sen.getentltore (FUNCTION)
  ← sen.getentlstatus4 (FUNCTION)
  ← sen.getentlstatus2 (FUNCTION)
  ← sen.getavailablesenfromstate (FUNCTION)
  ← sen.get (FUNCTION)
  ← sen.fistunfabgeschlossen (FUNCTION)
  ← sen.fistplused (FUNCTION)
  ← sen.fistindustriegut (FUNCTION)
  ← sen.fhasstatus (FUNCTION)
  ← sen.fhasnvescank (FUNCTION)
  ← sen.fgetnvescank (FUNCTION)
  ← sen.fcheckcomplete (FUNCTION)
  ← sen.fcancreatenewnl (FUNCTION)
  ← sen.copy (FUNCTION)
  ← sen.canmodify (FUNCTION)
  ← sen.fgetstellplatzc (FUNCTION)
  ← sen.getentllhm (FUNCTION)
  ← pvtmsnk.get3monthaverage (FUNCTION)
  ← pv_sen_pruef.canmodify (FUNCTION)
  ← ptms_hst.gettable (FUNCTION)
  ← pta_nv.actualizefrksen (PROCEDURE)
  ← pta_nv.getmaxprodinternk (FUNCTION)
  ← pta_nv.remsen (PROCEDURE)
  ← pta_nv.putdfltladort (PROCEDURE)
  ← pta_nv.paddsen (PROCEDURE)
  ← ppers_tb.istfixtpflicht (FUNCTION)
  ← plhmbuch.loopsen (PROCEDURE)
  ← plhmbuch.writeborderosenlhmbuch (FUNCTION)
  ← pkdfibu.readfakpos (PROCEDURE)
  ← pkdfibu.getzusasenn (FUNCTION)
  ← pextfaklst.importextfaklst (PROCEDURE)
  ← lstgen.erl_tb_anp (PROCEDURE)
  ← lstgen.auf_teil_neu (PROCEDURE)
  ← lstgen.erloes (PROCEDURE)
  ← lhm_auswertungmp.collectdata (PROCEDURE)
  ← faktur.faktime (PROCEDURE)
  ← disp_status.cntvol (PROCEDURE)
  ← disp_status.cntgew (PROCEDURE)
  ← disp_status.cntanz (PROCEDURE)
  ← disp_lesen.reservieren (PROCEDURE)
  ← disp_lesen.verarbsendung (PROCEDURE)
  ← disp_korr.relation (PROCEDURE)
  ← disp_korr.ladeliste (PROCEDURE)
  ← disp_hilf.unn_lst (PROCEDURE)
  ← disp_hilf.unf_lst (PROCEDURE)
  ← disp_hilf.gen_unf_lst (PROCEDURE)
  ← disp_hilf.calc_sperr (PROCEDURE)
  ← dfvtour.set (PROCEDURE)
  ← dfvtour.reorganisets (PROCEDURE)
  ← dfvtour.set1 (PROCEDURE)
  ← dbto_csmsentourpunkt.dbto_csmsentourpunkt (FUNCTION)
  ← dbto_csmsenhst.dbto_csmsenhst (FUNCTION)
  ← dbto_csmpsthst.dbto_csmpsthst (FUNCTION)
  ← caltms_sen.getorigsennfromhl (FUNCTION)
  ← cal_monitor.write_awdc (PROCEDURE)
  ← cal_control.getrsenstempel (FUNCTION)
  ← aggsenpstzustand.updatesen (PROCEDURE)
  ← tms1034.lst_k (TABLE)
  ← tms1034.sen_land (TABLE)
  ← tms1034.sen_pst (TABLE)
  ← tms1034.sen_frk_unt (TABLE)
  ← tms1034.lager_buch2aa (TABLE)
  ← tms1034.text_sendung (TABLE)
  ← tms1034.sen_zuord (TABLE)
  ← tms1034.sen_sammelauft (TABLE)
  ← tms1034.sendgort (TABLE)
  ← tms1034.sen_zoll (TABLE)
  ← tms1034.sen_ber (TABLE)
  ← tms1034.sen_ref (TABLE)
  ← tms1034.sen_zus (TABLE)
  ← tms1034.send_ls (TABLE)
  ← tms1034.sen_rrv (TABLE)
  ← tms1034.frk_sen (TABLE)
  ← tms1034.sen_hst (TABLE)
  ← tms1034.be_entlad_stelle (TABLE)
  ← tms1034.papier_sendung (TABLE)
  ← tms1034.sen_abh (TABLE)
  ← tms1034.sen_bahn (TABLE)
  ← tms1034.sendungspos_land (TABLE)
  ← tms1034.pers_sen (TABLE)
  ← tms1034.sen_tb (TABLE)
  ← tms1034.frb_protokoll (TABLE)
  ← tms1034.ta_sen_lst_b (TABLE)
  ← tms1034.sen_zustand (TABLE)
  ← pdriverterminal.gettransportorderinfolistdto (FUNCTION)
  ← dbto_mde_sen.fillobj (PROCEDURE)
  ← tms1034.aqsengesbordero (FUNCTION)
  ← tms1034.aqsengesentladen (FUNCTION)
  ← tms1034.aqsengesentladen_sa (FUNCTION)
  ← tms1034.aqsenmehrfentl01 (FUNCTION)
  ← ausl_quote.get (PROCEDURE)
  ← tms1034.aqsenmehrfentl03 (FUNCTION)
  ← tms1034.aqsenmehrfentl_sa (FUNCTION)
  ← tms1034.getsenfinalstate (FUNCTION)
  ← tms1034.xchisactivemasternve (FUNCTION)
  ← tms1034.delnotscannedabl (PROCEDURE)
  ← tms1034.reorgavists (PROCEDURE)
  ← aggsenpstzustand.insertpst (PROCEDURE)
  ← aggsenpstzustand.insertsen (PROCEDURE)
  ← aggsenpstzustand.insertsenpst (PROCEDURE)

STATISTICS:
- Direct dependencies: 2
- Total dependency paths: 4
- Objects depending on this: 916

DEPENDENCY COMPLEXITY:
- Simple (few dependencies)
- High impact object (many dependents)
```

---

## 🧑‍💼 User Query

Create a SQL script that utilizes createsendung to create test data from test_sendung_data.csv. I'm specifilly not sure how to pass the csv to the function in an SQL script.
